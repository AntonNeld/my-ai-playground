version: '3'
services:
  dungeon:
    build: "../dungeon/"
    environment:
      DUNGEON_PLAYER_AI: ai
    ports:
      - "5000:5000"
    volumes:
      - "../dungeon/:/code"
  ai:
    build: "../ai/random_movement/"
    volumes:
      - "../ai/random_movement/:/code"